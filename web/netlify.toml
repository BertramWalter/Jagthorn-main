[build]
  publish = "dist"
  command = "npm run build"

# SPA-fallback så client-side routing (/melodies/:id) virker ved genindlæsning.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
